nome
nome[1]
nome.length
concatenado
concatenado = nome + " " + sobrenome
concatenado = nome + "\n" + sobrenome
concatenado = nome + "\n\n" + sobrenome
concatenado = nome + '\n\n' + sobrenome
concatenado = `${nome} ${sobrenome}`
concatenado = `${nome} ${sobrenome}`
concatenado = "\""
let frase = "Ola, tudo bem?"
frase.split(" ")
frase.includes("tudo")
frase.startsWith("O")
frase.endsWith("?")
frase.replace(",","!")
let stringModificada = frase.replace(",","!")